require('../briefing');
```

(with `../` because briefing.js is one level up from the src folder)

**6 — Commit that too**

**7 — Go to Railway → Variables tab → add these:**
```
CLICKUP_TOKEN=pk_your_token_here
CLICKUP_WORKSPACE_ID=9014972154
TEST_MODE=true
TEST_SEND_NOW=true
